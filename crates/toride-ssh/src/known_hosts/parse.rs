//! Parse `known_hosts` files using ssh-key crate.

use crate::Result;

/// Parse a known_hosts file at the given path.
pub async fn parse_known_hosts(_path: &std::path::Path) -> Result<()> {
    todo!()
}
