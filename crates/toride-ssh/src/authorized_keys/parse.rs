//! Parse `authorized_keys` files using ssh-key crate.

use crate::Result;

/// Parse an authorized_keys file at the given path.
pub async fn parse_authorized_keys(_path: &std::path::Path) -> Result<()> {
    todo!()
}
